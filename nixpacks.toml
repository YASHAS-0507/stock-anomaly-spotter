[phases.setup]
aptPkgs = ["libgomp1"]

[phases.install]
cmds = [
    "python -m venv .venv && .venv/bin/pip install -r backend/requirements.txt",
    ".venv/bin/pip install smartapi-python==1.3.4",
]

[start]
cmd = ".venv/bin/uvicorn backend.app:app --host 0.0.0.0 --port $PORT"